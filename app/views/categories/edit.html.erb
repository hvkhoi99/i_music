<%= form_for(@category) do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="field">
    <label>Name:</label> <%= f.text_field :name, placeholder: "name home..." %>
  </div>
  
  <%= f.submit "Update", class: "btn btn-primary" %>
<% end %>
